services:
  bb-terminal:
    build:
      context: .
      args:
        FQDN: changeme.example.com
    ports:
      - "5173:5173"
    volumes:
      #- .:/app/BB-Terminal
      - ./openbb_platform:/root/.openbb_platform
    stdin_open: true
    tty: true
    restart: unless-stopped

